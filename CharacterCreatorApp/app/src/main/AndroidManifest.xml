<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.charactercreatorapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/travellerlogo"
        android:label="Traveller Character Creator"
        android:roundIcon="@drawable/travellerlogo"
        android:supportsRtl="true"
        android:theme="@style/Theme.CharacterCreatorApp">
        <activity android:name=".CreateNewCharActivity">
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>